# Eval Report: PR #9935

**Repo:** wolfssl/wolfssl
**PR Title:** Add IP SAN matching
**Problem:** Add IP SAN matching (source: pr_body)
**Generated:** 2026-03-22T09:04:49Z
**Verdict:** improved

## Scores

| Metric | Bare | wolfDen | wolfDen (iter 1) | wolfDen Delta |
| --- | --- | --- | --- | --- |
| Root Cause Identification | 0.00 | 0.00 | 0.00 | +0.00 |
| File Targeting | 0.00 | 0.00 | 0.00 | +0.00 |
| Knowledge Application | 0.00 | 0.00 | 0.00 | +0.00 |
| Fix Quality | 0.00 | 0.00 | 0.00 | +0.00 |
| Investigation Methodology | 0.00 | 0.00 | 0.00 | +0.00 |
| **Composite** | **0.661** | **0.426** | **0.787** | **+0.126** |

## Analysis Details

### Baseline (bare: 0.661, wolfden: 0.426)

**Summary:** The task required implementing wolfSSL_check_ip_address() as a new public API mirroring wolfSSL_check_domain_name(), with changes across 7 files: header declaration (ssl.h), internal struct field (internal.h with 'ipasc' buffer), implementation (ssl.c with OPENSSL_EXTRA compat path), verification integration (internal.c with #ifndef OPENSSL_EXTRA guard), cleanup (ResourceFree), two separate test functions (one for OpenSSL param path, one for convenience API), and bilingual doxygen docs.

Bare Claude produced an implementation summary that correctly identified 6 of 7 files and the core pattern (store IP, check during verification, clean up in ResourceFree). It missed the OPENSSL_EXTRA dual-path architecture — the PR carefully guards the native verification check with #ifndef OPENSSL_EXTRA and adds a separate wolfSSL_X509_VERIFY_PARAM_set1_ip_asc call for the compat path. It also named the buffer field 'ipAddr' instead of 'ipasc' and wrote only one test instead of two.

wolfDen Claude produced no implementation at all, concluding the feature was already complete. It demonstrated superior architectural understanding — correctly identifying the dual verification paths, CheckIPAddr delegation, error codes, and configure flags — but this knowledge led to overconfidence rather than productive output. Whether it was reading post-merge code or hallucinating, the result was a total task failure.

The key systemic gap is documentation of wolfSSL's dual verification architecture: features touching certificate validation must integrate with both the native path (ssl->buffers + ProcessPeerCerts) and the OPENSSL_EXTRA path (ssl->param + X509_VERIFY_PARAM), with guards to prevent double-checking. This pattern is non-obvious and affects any future certificate validation feature.

### Iteration 1 (bare: 0.497, wolfden: 0.787)

**Summary:** The task required adding wolfSSL_check_ip_address() as a public API mirroring wolfSSL_check_domain_name(), touching 7 files: public header declaration (ssl.h, no feature guard), internal buffer storage (internal.h, 'ipasc' field), implementation with OPENSSL_EXTRA forwarding (ssl.c), verification integration with #ifndef OPENSSL_EXTRA guard plus a bug fix for CheckForAltNames (internal.c), resource cleanup, two separate unit tests for each verification path, and bilingual doxygen.

Bare Claude correctly identified the core pattern — store IP in ssl->buffers, verify during ProcessPeerCerts, clean up in ResourceFree — and got most files right. However, it missed the critical dual-path architecture: the PR carefully separates native verification (#ifndef OPENSSL_EXTRA using CheckIPAddr) from the OpenSSL compat path (forwarding to X509_VERIFY_PARAM_set1_ip_asc). It also used the wrong buffer name ('ipAddr' vs 'ipasc'), incorrectly guarded the declaration with WOLFSSL_IP_ALT_NAME, missed the CheckForAltNames bug fix, and wrote only one test.

wolfDen Claude produced a substantially more accurate implementation matching the PR on nearly every detail: correct buffer naming, dual-path architecture with proper guards and rationale, the bug fix, two test functions with appropriate #ifdef guards and stub fallbacks, and complete file coverage. The improvement from iteration 0 (where wolfDen failed entirely by claiming the feature existed) to this iteration is dramatic.

The key systemic knowledge that made the difference is understanding wolfSSL's dual certificate verification architecture: any feature touching cert validation must integrate with both the native ProcessPeerCerts path and the OPENSSL_EXTRA DoVerifyCallback path, with mutual exclusion guards to prevent double-checking.

**Changes applied:**
- `scaffold/.claude/rules/crypto/certificates.md` — Any developer adding a new certificate verification parameter (pinned key, CT policy, etc.) needs to know about the mutual-exclusion guard between native and compat paths. This is a general architectu

