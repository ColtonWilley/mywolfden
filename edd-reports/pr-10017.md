# Eval Report: PR #10017

**Repo:** wolfssl/wolfssl
**PR Title:** Fix ssl_DecodePacketInternal chain processing
**Problem:** Fix ssl_DecodePacketInternal chain processing (source: pr_body_section)
**Generated:** 2026-03-22T12:56:11Z
**Verdict:** improved

## Scores

| Metric | Bare | wolfDen | wolfDen (iter 1) | wolfDen Delta |
| --- | --- | --- | --- | --- |
| Root Cause Identification | 0.00 | 0.00 | 0.00 | +0.00 |
| File Targeting | 0.00 | 0.00 | 0.00 | +0.00 |
| Knowledge Application | 0.00 | 0.00 | 0.00 | +0.00 |
| Fix Quality | 0.00 | 0.00 | 0.00 | +0.00 |
| Investigation Methodology | 0.00 | 0.00 | 0.00 | +0.00 |
| **Composite** | **0.463** | **0.215** | **0.482** | **+0.020** |

## Analysis Details

### Baseline (bare: 0.463, wolfden: 0.215)

**Summary:** The task was a security fix in wolfSSL's sniffer module: the chain accumulation loop in ssl_DecodePacketInternal summed size_t iov_len values into an int, causing silent truncation and heap overflow. The ground truth fix uses size_t totalLength with both a wraparound check (totalLength < prev) and an INT_MAX bounds check, calls SetError() with BAD_INPUT_STR before returning, and adds the test inline in tests/api.c using TEST_DECL.

Bare Claude proposed a reasonable fix (size_t accumulation + INT_MAX check + cast) but missed the wraparound check and SetError() pattern. It incorrectly proposed separate test files instead of inline in api.c, and over-scoped the build system changes. Despite these issues, it delivered an actionable implementation plan.

wolfDen Claude catastrophically failed by hallucinating that the fix was already implemented. It cited specific line numbers, file contents, and macro names for code that did not exist in the pre-PR codebase. Its domain knowledge about wolfSSL patterns (DYNAMIC_TYPE constants, ifdef guards, test macros) provided just enough scaffolding to construct a convincing but fabricated description. This is the worst outcome: domain knowledge created false confidence that bypassed verification.

The key systemic gap is that wolfDen's knowledge can induce confabulation — knowing wolfSSL patterns well enough to generate plausible code descriptions without needing to actually read the code. The coding-standards documentation also misled on test organization (TEST_DECL_GROUP in separate files vs. inline TEST_DECL). Missing knowledge includes the SetError() error-reporting pattern for sniffer functions and standard integer overflow defense idioms.

### Iteration 1 (bare: 0.490, wolfden: 0.482)

**Summary:** This task required fixing a heap overflow in wolfSSL's sniffer chain processing where size_t iov_len values accumulated into an int caused silent truncation. The ground truth fix uses size_t totalLength with dual guards (unsigned wraparound via prev comparison AND INT_MAX bounds check), calls SetError() for proper sniffer error reporting, and adds the test inline in tests/api.c with WOLFSSL_SNIFFER ifdef guards.

Bare Claude correctly identified the size_t→int truncation and proposed size_t accumulation with INT_MAX checking, but missed the wraparound guard entirely and incorrectly proposed separate test files instead of inline in api.c. It over-scoped build system changes but provided a reasonable evidence trail.

wolfDen Claude correctly identified the wraparound vulnerability (the more subtle bug) and proposed the prevLen comparison pattern matching the ground truth. However, its response was extremely terse with no visible evidence of code verification — a concerning pattern after iteration 0's hallucination failure. It didn't address test file placement or SetError() usage, and added an unnecessary SIZE_MAX edge case.

The key improvement this iteration: wolfDen no longer hallucinated that the fix existed. It proposed the correct wraparound check. But it still lacks evidence discipline and missed the SetError() sniffer error pattern. The systemic gaps are: (1) sniffer module error reporting patterns (SetError with BAD_INPUT_STR), (2) test organization conventions (inline api.c vs separate files), and (3) standard C integer overflow defense idioms should be in coding standards documentation so both the wraparound and truncation checks are caught together.

**Changes applied:**
- `scaffold/.claude/rules/coding-standards.md` — The current content only describes the TEST_DECL_GROUP pattern and claims api.c never needs modification. In reality, ~400 tests use the simpler TEST_DECL inline pattern where api.c IS modified. This 
- `scaffold/.claude/rules/discovered-patterns.md` — Both responses missed the wraparound detection idiom (totalLength < prev) which is a standard integer overflow defense for accumulation loops. The existing 'Unsigned Arithmetic Guards in ASN Parsing' 

