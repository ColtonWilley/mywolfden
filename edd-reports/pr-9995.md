# Eval Report: PR #9995

**Repo:** wolfssl/wolfssl
**PR Title:** Handle OCSP_WANT_READ returned from DoTls13HandShakeMsgType
**Problem:** Handle OCSP_WANT_READ returned from DoTls13HandShakeMsgType (source: pr_body)
**Generated:** 2026-03-22T15:58:08Z
**Verdict:** neutral

## Scores

| Metric | Bare | wolfDen | wolfDen (iter 1) | wolfDen Delta |
| --- | --- | --- | --- | --- |
| Root Cause Identification | 0.00 | 0.00 | 0.00 | +0.00 |
| File Targeting | 0.00 | 0.00 | 0.00 | +0.00 |
| Knowledge Application | 0.00 | 0.00 | 0.00 | +0.00 |
| Fix Quality | 0.00 | 0.00 | 0.00 | +0.00 |
| Investigation Methodology | 0.00 | 0.00 | 0.00 | +0.00 |
| **Composite** | **0.537** | **0.770** | **0.775** | **+0.238** |

## Analysis Details

### Baseline (bare: 0.537, wolfden: 0.770)

**Summary:** The task required fixing DoTls13HandShakeMsg in tls13.c to preserve the pendingMsg buffer when OCSP_WANT_READ is returned during non-blocking OCSP verification of fragmented certificate messages. The fix is a 3-line change: expanding the #ifdef guard to include WOLFSSL_NONBLOCK_OCSP and adding OCSP_WANT_READ to the retry condition. The full PR also updated the test harness (tests/api.c) to treat OCSP_WANT_READ as non-fatal, added a regression test using a 2-cert chain with MFL=1024 to force fragmentation, and updated CI to test with WOLFSSL_NONBLOCK_OCSP and --enable-maxfragment.

Both Claudes correctly identified the core tls13.c fix. Bare Claude's major error was claiming internal.c (the TLS 1.2 path) also needed the fix, when it already had WOLFSSL_NONBLOCK_OCSP in its guard — a false positive from assuming symmetry without verification. wolfDen correctly distinguished the two paths and identified only tls13.c as needing the fix.

wolfDen showed stronger investigation methodology by finding the upstream commit and identifying the full change scope (tests, CI, harness). However, it presented these as existing artifacts rather than proposing to implement them. Neither response proposed writing the test_ssl_memio_do_handshake changes in api.c, which are required for the regression test to work.

The key systemic gap is documentation of the TLS 1.2/1.3 handshake path asymmetries — specifically which retry conditions each path already handles. A developer encountering a bug in one path naturally assumes the other has the same bug, but wolfSSL's incremental development means fixes land asymmetrically. The test harness's own error-handling requirements (separate from the library fix) are also undocumented.

### Iteration 1 (bare: 0.458, wolfden: 0.775)

**Summary:** The task required fixing DoTls13HandShakeMsg in tls13.c to preserve the pendingMsg buffer when OCSP_WANT_READ is returned during non-blocking OCSP verification of fragmented certificate messages. The core fix is a 3-line change: expanding the #ifdef guard to include WOLFSSL_NONBLOCK_OCSP and adding OCSP_WANT_READ to the retry condition. The full PR also updated test_ssl_memio_do_handshake in api.c to treat OCSP_WANT_READ as non-fatal, added a regression test exercising TLS 1.3 + non-blocking OCSP + low MFL to force fragmentation, and updated CI configuration.

Bare Claude correctly identified the tls13.c fix but made a significant false positive by also claiming internal.c needed the same fix — the TLS 1.2 path already had WOLFSSL_NONBLOCK_OCSP in its guard. This demonstrates the danger of assuming symmetry between the two handshake paths without verification. Bare Claude also missed all test and CI scope.

wolfDen correctly scoped the fix to only tls13.c, found the upstream commit that fixed the non-fragmented path, and identified all five files that needed changes (tls13.c, api.c harness, test_ocsp.c, test_ocsp.h, os-check.yml). It even named the test function correctly. However, it described these changes at a high level rather than proposing concrete implementation code.

The key systemic gap is the asymmetric evolution of TLS 1.2 and TLS 1.3 handshake retry handling. Fixes land incrementally and the two paths diverge in which conditions they already handle. wolfDen's investigation methodology helped navigate this, but explicit documentation of which retry conditions each path handles would prevent the false-positive pattern bare Claude exhibited.

**Changes applied:**
- `scaffold/.claude/rules/crypto/tls-handshake.md` — The core failure mode was assuming TLS 1.2 and 1.3 share the same retry-condition handling. Any developer fixing handshake bugs, adding new async/non-blocking error codes, or working on fragmented mes
- `scaffold/.claude/rules/discovered-patterns.md` — This is a recurring pattern for any developer adding new async/non-blocking error codes to wolfSSL. The test harness has its own error-handling gates independent of the library, and forgetting to upda

