# Eval Report: PR #10023

**Repo:** wolfssl/wolfssl
**PR Title:** ASM generation fixes
**Problem:** ASM generation fixes (source: pr_body)
**Generated:** 2026-03-22T12:28:51Z
**Verdict:** regressed

## Scores

| Metric | Bare | wolfDen | wolfDen (iter 1) | wolfDen Delta |
| --- | --- | --- | --- | --- |
| Root Cause Identification | 0.00 | 0.00 | 0.00 | +0.00 |
| File Targeting | 0.00 | 0.00 | 0.00 | +0.00 |
| Knowledge Application | 0.00 | 0.00 | 0.00 | +0.00 |
| Fix Quality | 0.00 | 0.00 | 0.00 | +0.00 |
| Investigation Methodology | 0.00 | 0.00 | 0.00 | +0.00 |
| **Composite** | **0.657** | **0.740** | **0.575** | **-0.083** |

## Analysis Details

### Baseline (bare: 0.657, wolfden: 0.740)

**Summary:** This task involved a large batch of ASM generation fixes spanning 22 files across x86_64, ARM32, ARM64 architectures — mixing functional bugs (AES-XTS register clobber, SP math wrong source registers), invalid mnemonics (NEON vrev32.i16), and cosmetic fixes (label typos, comment corrections). Both Claudes correctly identified all major fix categories and produced accurate summaries. Bare Claude claimed to have applied all fixes and provided reasonable detail, though it inflated some instance counts (40 vs actual ~6 for the r11→r7 fix) and missed the sp_x86_64 files entirely. wolfDen Claude found the existing commit on master and recommended cherry-picking — pragmatically correct but somewhat sidestepping independent investigation. wolfDen's key advantages were: discovering the sp_x86_64 files not mentioned in the description, providing accurate counts, and adding security impact analysis showing understanding of what AES-XTS round selection and Montgomery reduction correctness mean for crypto operations. Neither Claude deeply explained ARM NEON mnemonic rules or the code generator relationship between platform-specific assembly files. The domain knowledge gap here is about wolfSSL's ASM code generation pipeline — understanding that sp_arm32.c, sp_cortexm.c, and sp_x86_64_asm.S are parallel outputs means fixes in one should trigger checking all variants. wolfDen's advantage was modest but real, primarily in completeness and crypto impact awareness rather than in investigation methodology.

### Iteration 1 (bare: 0.575, wolfden: 0.575)

**Summary:** This task required applying a batch of ASM fixes across 22 files spanning x86_64, ARM32, and ARM64 architectures. Fixes ranged from functional bugs (AES-XTS register clobber in AVX1 init, SP math wrong source registers) to cosmetic corrections (label typos, comment fixes, NEON mnemonic syntax). The key challenge was correctly identifying all fix sites across paired file formats (.S assembly, _c.c inline asm, .asm MASM) and understanding the specific bug patterns.

Bare Claude produced a comprehensive and mostly accurate summary covering all fix categories, though it inflated instance counts (claiming 40 r11→r7 fixes vs the actual ~6) and provided no evidence of code verification. Its strength was systematic coverage of all described categories without introducing incorrect changes.

wolfDen Claude showed deeper architectural awareness — distinguishing encrypt vs decrypt paths, understanding AESNI vs AVX1 — but critically misidentified the AES-XTS bug pattern. It claimed to fix 'movl %eax, %r11d' in encrypt/encrypt_update functions, while the actual PR only removes 'movl %edx, %eax' in the init_avx1 function. This is a case where partial domain knowledge induced false confidence: wolfDen 'knew' about AES-XTS structure and projected fixes onto the wrong functions. Its 'pre-existing' claims for other fixes also lacked clear verification.

The systemic gap is documentation of wolfSSL's ASM code generation pipeline — understanding that sp_arm32.c, armv8-32-*-asm.S, and their _c.c counterparts are coordinated outputs, and that fixes must be applied consistently across all variants. Neither Claude demonstrated understanding of this pipeline relationship, though both achieved reasonable coverage through the explicit file list in the task description.

**Changes applied:**
- `scaffold/.claude/rules/crypto/asm-code-generation.md` — When a developer encounters or reviews a bug fix in one SP math assembly file, they need to know that the same algorithmic bug may exist in all sibling platform variants. This cross-checking pattern a
- `scaffold/.claude/rules/crypto/asm-algorithm-optimization.md` — Developers reviewing ARM NEON assembly changes need to understand when a mnemonic suffix fix is cosmetic vs. functional. The distinction between element-size suffixes (.16) and type-qualifier suffixes

