# Eval Report: PR #10018

**Repo:** wolfssl/wolfssl
**PR Title:** Fix GetSafeContent to check length
**Problem:** Fix GetSafeContent to check length (source: pr_body)
**Generated:** 2026-03-22T12:41:41Z
**Verdict:** neutral

## Scores

| Metric | Bare | wolfDen | wolfDen (iter 1) | wolfDen Delta |
| --- | --- | --- | --- | --- |
| Root Cause Identification | 0.00 | 0.00 | 0.00 | +0.00 |
| File Targeting | 0.00 | 0.00 | 0.00 | +0.00 |
| Knowledge Application | 0.00 | 0.00 | 0.00 | +0.00 |
| Fix Quality | 0.00 | 0.00 | 0.00 | +0.00 |
| Investigation Methodology | 0.00 | 0.00 | 0.00 | +0.00 |
| **Composite** | **0.600** | **0.890** | **0.880** | **+0.280** |

## Analysis Details

### Baseline (bare: 0.600, wolfden: 0.890)

**Summary:** This task required fixing an unsigned integer underflow in PKCS12 ASN parsing — specifically in GetSafeContent() where ci->dataSz is computed as (word32)curSz - (localIdx - curIdx). A malformed ContentInfo SEQUENCE declaring fewer bytes than its contained OID actually consumes causes the subtraction to wrap to ~4GB. The fix is a bounds check after GetObjectId(), and the test requires a crafted DER blob plus proper test registration.

Bare Claude identified the correct file, function, and general approach (bounds check after GetObjectId, return ASN_PARSE_E), but made two notable mistakes: it used the subtraction-based condition form (localIdx - curIdx) > (word32)curSz which could itself underflow in edge cases, and it proposed corrupting an existing PKCS12 file for testing rather than crafting a minimal DER. Most critically, it only mentioned 2 of 3 changed files, completely missing test_pkcs12.h where the test declaration and TEST_PKCS12_DECLS macro registration are required.

wolfDen Claude produced output essentially identical to the ground truth PR across all three files. It used the overflow-safe condition form (localIdx > curIdx + (word32)curSz) and explicitly articulated why this is safer. It crafted a minimal DER test vector matching the PR's structure, and correctly updated test_pkcs12.h including the DECLS macro. wolfDen's advantage was clearly in wolfSSL test infrastructure patterns (knowing about test_pkcs12.h, EXPECT_DECLS, TEST_DECL_GROUP), defensive unsigned arithmetic idioms in ASN code, and the freeSafe cleanup pattern. The key systemic gap for bare Claude is unfamiliarity with wolfSSL's test registration architecture — tests require both implementation in .c files and declaration/registration in companion .h files with specific macros.

### Iteration 1 (bare: 0.522, wolfden: 0.880)

**Summary:** This task required fixing an unsigned integer underflow in PKCS12 ASN.1 parsing where GetObjectId() could consume more bytes than a ContentInfo SEQUENCE declared, causing ci->dataSz to wrap to ~4GB. The fix is a bounds check after GetObjectId() returning ASN_PARSE_E, plus a crafted-DER regression test registered across three files.

Bare Claude correctly identified the vulnerable function and the general fix approach but made three significant errors: (1) used the subtraction-form condition `(localIdx - curIdx) > (word32)curSz` which could itself underflow with unsigned arithmetic in edge cases, (2) proposed corrupting an existing PKCS12 file rather than crafting a minimal DER test vector, and (3) completely missed test_pkcs12.h where test declarations and TEST_DECL_GROUP macro registration are required — the test would not compile without this file.

wolfDen Claude produced output nearly identical to the ground truth across all three files. It used the overflow-safe addition-form check (`localIdx > curIdx + (word32)curSz`), crafted a minimal DER matching the PR's structure with annotated byte-level comments, correctly updated test_pkcs12.h with both the declaration and DECLS macro, and included the freeSafe cleanup before the early return. wolfDen explicitly cited its discovered-patterns knowledge for the unsigned arithmetic idiom.

The key systemic gap for bare Claude is wolfSSL's test registration architecture — the pattern where tests require companion .h file updates with specific macros is not discoverable without either reading existing test files carefully or having domain knowledge. wolfDen's loaded rules about test infrastructure, ASN defensive coding patterns, and memory cleanup conventions directly addressed the three areas where bare Claude fell short.

**Changes applied:**
- `scaffold/.claude/rules/coding-standards.md` — The companion header pattern (test_FEATURE.h with TEST_DECL_GROUP macros) is completely non-obvious from reading any single file. This was the single biggest gap in the PR analysis — bare Claude misse
- `scaffold/.claude/rules/discovered-patterns.md` — Unsigned integer underflow in size computations is a recurring vulnerability class in ASN.1 parsing code. The addition-form bounds check pattern (check before subtracting rather than checking the resu

