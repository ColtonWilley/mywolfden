# Eval Report: PR #9993

**Repo:** wolfssl/wolfssl
**PR Title:** Brainpool to set1_sigalgs_list
**Problem:** Brainpool to set1_sigalgs_list (source: pr_body)
**Generated:** 2026-03-22T16:12:01Z
**Verdict:** improved

## Scores

| Metric | Bare | wolfDen | wolfDen (iter 1) | wolfDen Delta |
| --- | --- | --- | --- | --- |
| Root Cause Identification | 0.00 | 0.00 | 0.00 | +0.00 |
| File Targeting | 0.00 | 0.00 | 0.00 | +0.00 |
| Knowledge Application | 0.00 | 0.00 | 0.00 | +0.00 |
| Fix Quality | 0.00 | 0.00 | 0.00 | +0.00 |
| Investigation Methodology | 0.00 | 0.00 | 0.00 | +0.00 |
| **Composite** | **0.580** | **0.560** | **0.663** | **+0.083** |

## Analysis Details

### Baseline (bare: 0.580, wolfden: 0.560)

**Summary:** The task required adding brainpool curve support to wolfSSL's sigalgs_list API by adding an entry to the sig_names[] lookup table in src/internal.c. The correct implementation was a single 3-line addition: { ecc_brainpool_sa_algo, "ECDSA-BRAINPOOL" } guarded by #ifdef HAVE_ECC_BRAINPOOL, nested inside the existing #ifdef HAVE_ECC block.

Bare Claude correctly identified sig_names[] as the integration point and that no entry existed, but chose the wrong string name "BRAINPOOL" instead of "ECDSA-BRAINPOOL". This would work functionally but breaks the naming convention used by other entries (RSA-PSS, ECDSA) and wouldn't match what users expect from OpenSSL compatibility.

wolfDen Claude got the string name right ("ECDSA-BRAINPOOL") and correctly identified the placement and naming convention. However, it hallucinated that a "BRAINPOOL" entry already existed in sig_names[] and framed the fix as adding an alias. The ground truth shows no brainpool entry existed before the PR — the entire entry was new. This is the classic domain-knowledge-induced false confidence pattern: knowing that brainpool is supported elsewhere in wolfSSL (AddSuiteHashSigAlgo, wire codes) led wolfDen to assume a sig_names entry must also exist.

The key systemic gap is that wolfDen's domain knowledge about wolfSSL's naming conventions was helpful for getting the string right, but its broader knowledge about brainpool support created a false assumption about existing code state. The lesson: domain knowledge must enhance code reading, not replace it. wolfDen should have verified the current sig_names[] contents before asserting what was already there.

### Iteration 1 (bare: 0.575, wolfden: 0.663)

**Summary:** The task required adding brainpool curve support to wolfSSL's sigalgs_list parsing API by adding a single entry to the sig_names[] lookup table in src/internal.c. The correct implementation was 3 lines: { ecc_brainpool_sa_algo, "ECDSA-BRAINPOOL" } guarded by #ifdef HAVE_ECC_BRAINPOOL, nested inside #ifdef HAVE_ECC.

Both Claudes correctly identified sig_names[] as the sole integration point and correctly noted that AddSuiteHashSigAlgo() already handles brainpool — meaning no downstream changes were needed. This architectural reasoning was solid from both.

The key differentiator was the string name. Bare Claude chose "BRAINPOOL" which doesn't follow the naming convention visible in the existing table ("ECDSA", "RSA-PSS", "ED25519" — algorithm-family prefixed names). wolfDen correctly chose "ECDSA-BRAINPOOL" as the primary name, matching the convention. However, wolfDen hedged by also adding a "BRAINPOOL" alias that wasn't in the ground truth, suggesting incomplete confidence.

Neither response exhibited the hallucination problem from iteration 0 — both correctly treated the brainpool entry as new rather than existing. Both proposed test additions beyond the actual PR scope, which is reasonable practice but shows neither calibrated to the minimal-fix nature of the task.

The systemic gap remains: wolfDen's domain knowledge helped with naming conventions but doesn't explicitly document the sig_names[] string naming pattern or OpenSSL compatibility string formats. Adding explicit documentation about how wolfSSL maps algorithm names in its OpenSSL compatibility layer (particularly the prefix convention in sig_names[]) would help future tasks in this area. Evidence discipline was similar for both — neither showed strong verification of claims against code reads in their responses.

**Changes applied:**
- `scaffold/.claude/rules/investigation-methods.md` — The critical failure was assuming sig_names[] had a brainpool entry because brainpool was supported in AddSuiteHashSigAlgo. This applies broadly: wolfSSL has many parallel lookup structures (sig_names
- `scaffold/.claude/rules/discovered-patterns.md` — Bare Claude chose 'BRAINPOOL' instead of 'ECDSA-BRAINPOOL' because the key-type prefix convention isn't documented. Any developer adding a new algorithm to sigalgs_list, cipher_list, or similar string

