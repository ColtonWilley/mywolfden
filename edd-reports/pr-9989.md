# Eval Report: PR #9989

**Repo:** wolfssl/wolfssl
**PR Title:** add sanity check on keysize found with ECC point import
**Problem:** add sanity check on keysize found with ECC point import (source: pr_body)
**Generated:** 2026-03-22T20:05:52Z
**Verdict:** neutral

## Verdicts

| Pillar | Bare | wolfDen | wolfDen (iter 1) |
| --- | --- | --- | --- |
| **Overall** | **needs_improvement** | **pass** | **pass** |

## Analysis Details

### Baseline (bare: needs_improvement, wolfden: pass)

**Summary:** This task required adding an input validation sanity check in wc_ecc_import_point_der_ex() to verify that the keysize derived from the input buffer length matches the expected curve size (ecc_sets[curve_idx].size). The complete solution had two parts: the validation check in wolfcrypt/src/ecc.c and three negative test cases in wolfcrypt/test/test.c.

Both Claude sessions correctly identified the core fix: a keysize != ecc_sets[curve_idx].size check returning ECC_BAD_ARG_E, placed after keysize computation and before mp_read_unsigned_bin(), guarded by the err == MP_OKAY pattern. Both correctly identified the file, function, and exact insertion point around line 9490. Data accuracy was solid for both — claims about function names, line numbers, error codes, and code patterns appear grounded in actual code reading.

The critical difference was completeness. Bare Claude only modified ecc.c and did not add any tests. wolfDen Claude matched the full PR scope by adding three test cases: two compressed-point imports with only 1 byte of input (using existing derComp0 and derComp1 test vectors), and one full-length 65-byte buffer with an invalid prefix byte 0x01. These tests were correctly placed inside the HAVE_COMP_KEY conditional block and used wolfSSL's standard negative-test patterns (WC_NO_ERR_TRACE wrapper, WC_TEST_RET_ENC_EC error encoding, goto done flow).

wolfDen's advantage appears to stem from two sources: (1) familiarity with wolfSSL's convention of always pairing security-relevant validation fixes with negative test cases in the existing test infrastructure, and (2) potentially finding the upstream commit (44de734fa) that this change ports, which would have provided a complete template. The domain knowledge about test file structure, #ifdef guard combinations for FIPS compatibility, and wolfSSL-specific test macros enabled wolfDen to produce a complete, production-ready change.

The systemic gap for bare Claude is understanding wolfSSL's testing expectations for security-relevant input validation changes. Any fix that adds bounds checking or input validation in wolfcrypt should be accompanied by negative test cases exercising the new error path. This convention, along with the specific test patterns and guard macros used in wolfcrypt/test/test.c, would help future sessions produce complete changes.

### Iteration 1 (bare: needs_improvement, wolfden: pass)

**Summary:** This task required adding an input validation check in wc_ecc_import_point_der_ex() to verify that the keysize derived from the input buffer matches the curve's expected size (ecc_sets[curve_idx].size), plus negative test cases exercising the new error path. The ground truth PR has two files: a 7-line check in wolfcrypt/src/ecc.c and three test cases in wolfcrypt/test/test.c (two truncated compressed points, one invalid prefix byte).

Both sessions correctly identified the core fix: a keysize != ecc_sets[curve_idx].size comparison returning ECC_BAD_ARG_E, placed after keysize computation and before mp_read_unsigned_bin(), guarded by err == MP_OKAY. Both correctly named the file, function, insertion point (~line 9490), and error code. Both noted that wc_ecc_import_x963_ex2() already performs analogous validation via wc_ecc_set_curve(). Data accuracy was solid for both — no hallucinated functions, files, or code patterns.

The decisive difference was completeness. Bare Claude only produced the ecc.c fix with no tests. wolfDen Claude matched the full PR scope: the same ecc.c fix plus four negative test cases (the ground truth's three plus one additional case for uncompressed-path keysize mismatch). wolfDen's tests were correctly placed inside the HAVE_COMP_KEY guard block, used existing derComp0/derComp1 test vectors, and followed wolfSSL's standard negative-test patterns.

wolfDen's advantage stems from domain knowledge about wolfSSL's convention that security-relevant input validation changes must include negative test cases, and familiarity with the specific test infrastructure patterns in wolfcrypt/test/test.c. This is consistent with the previous iteration's finding. The systemic gap for bare Claude remains the same: without knowledge of wolfSSL's testing expectations for validation fixes, it produces incomplete changes that would require manual test additions before merging.

**Changes applied:**
- `scaffold/.claude/rules/coding-standards.md` — The root cause was that bare Claude didn't produce negative test cases for a security validation fix. wolfcrypt/test/test.c has distinct conventions from tests/api.c (different macros, flow patterns, 

