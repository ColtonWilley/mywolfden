# Eval Report: PR #9997

**Repo:** wolfssl/wolfssl
**PR Title:** add back WOLFSSL_QT macro guard for get cipher name behavior
**Problem:** add back WOLFSSL_QT macro guard for get cipher name behavior (source: pr_body)
**Generated:** 2026-03-22T15:05:39Z
**Verdict:** regressed

## Scores

| Metric | Bare | wolfDen | wolfDen (iter 1) | wolfDen Delta |
| --- | --- | --- | --- | --- |
| Root Cause Identification | 0.00 | 0.00 | 0.00 | +0.00 |
| File Targeting | 0.00 | 0.00 | 0.00 | +0.00 |
| Knowledge Application | 0.00 | 0.00 | 0.00 | +0.00 |
| Fix Quality | 0.00 | 0.00 | 0.00 | +0.00 |
| Investigation Methodology | 0.00 | 0.00 | 0.00 | +0.00 |
| **Composite** | **0.055** | **0.762** | **0.520** | **+0.465** |

## Analysis Details

### Baseline (bare: 0.055, wolfden: 0.762)

**Summary:** This task required restoring a WOLFSSL_QT preprocessor guard that PR #9831 inadvertently removed, causing Qt builds to return IANA cipher names instead of the internal/OpenSSL-style names Qt expects. The fix adds `!defined(WOLFSSL_QT)` to three #ifdef guards: one in wolfSSL_CIPHER_get_name() in src/ssl.c, and two in tests/api.c (test_wolfSSL_get_client_ciphers and test_wolfSSL_crypto_policy_ciphers).

Bare Claude provided a single dismissive sentence ('Background task cleanup — no action needed. The fix is already applied.') with zero technical detail, no evidence of reading any files, no explanation of the problem or solution, and no demonstration of understanding. This is functionally useless for evaluation purposes.

wolfDen Claude provided an accurate, detailed analysis matching the ground truth almost exactly: it correctly explained WHY Qt needs internal names (not IANA), identified all three change locations across both files, described what each guard does, and referenced the correct PR number (#9997). The line numbers were slightly off (likely due to working tree differences), and it claimed the fix was already applied without clear evidence in git status, but the technical content was sound.

The wolfDen advantage here was significant — understanding WOLFSSL_QT's role in the cipher name resolution path and the #ifdef guard pattern is domain-specific knowledge that helped identify the complete scope (including two test locations that a naive fix might miss). The key systemic pattern is that wolfSSL compatibility macros like WOLFSSL_QT affect behavior throughout multiple code paths, and changes to shared preprocessor guards must be audited across both implementation and test code.

### Iteration 1 (bare: 0.000, wolfden: 0.520)

**Summary:** This task required restoring a `!defined(WOLFSSL_QT)` guard to three `#ifdef` conditions that PR #9831 inadvertently removed: one in `wolfSSL_CIPHER_get_name()` (src/ssl.c) controlling whether IANA or internal cipher names are returned, and two matching guards in tests/api.c that adjust test expectations accordingly. The domain knowledge required includes understanding that Qt integration expects OpenSSL-compatible (internal) cipher names rather than IANA names, and that wolfSSL's preprocessor guard compounds must be audited across both implementation and test code.

Bare Claude produced a single dismissive sentence with zero technical content, zero evidence of reading code, and no demonstration of understanding the problem. It is functionally useless.

wolfDen Claude provided accurate technical analysis: it correctly explained the IANA vs internal name distinction for Qt builds, identified all three change locations matching the ground truth, and referenced the relevant commit. However, it critically failed to produce an implementation, instead claiming the fix was already applied. Its line numbers were also off from the PR diff, raising questions about verification rigor. Despite this execution failure, the underlying domain understanding was sound — it knew WHERE to look, WHAT to change, and WHY Qt needs different behavior.

The key systemic gap is twofold: (1) no methodology guidance for treating prompts as implementation tasks rather than status queries, and (2) the WOLFSSL_QT compatibility macro's cross-cutting effects on cipher name resolution are not well-documented as requiring coordinated changes across source and test files. The wolfDen knowledge clearly helped identify scope and understand the Qt-specific behavior, but couldn't overcome the fundamental failure to actually write the code.

**Changes applied:**
- `scaffold/.claude/rules/investigation-methods.md` — The root cause failure was claiming a fix was 'already applied' without verifying via git status or reading the actual file. This is a general methodology gap — any task involving PRs, patches, or reg
- `scaffold/.claude/rules/crypto/compat-layer.md` — The PR failure was a regression from accidentally removing a WOLFSSL_QT guard during refactoring. This pattern — application-specific compat macros modifying API behavior via #ifdef guards in ssl.c — 

