# Eval Report: PR #9998

**Repo:** wolfssl/wolfssl
**PR Title:** 20260317-wc_linuxkm_rng_is_wolfcrypt
**Problem:** 20260317-wc_linuxkm_rng_is_wolfcrypt (source: pr_body)
**Generated:** 2026-03-22T14:46:14Z
**Verdict:** improved

## Scores

| Metric | Bare | wolfDen | wolfDen (iter 1) | wolfDen Delta |
| --- | --- | --- | --- | --- |
| Root Cause Identification | 0.00 | 0.00 | 0.00 | +0.00 |
| File Targeting | 0.00 | 0.00 | 0.00 | +0.00 |
| Knowledge Application | 0.00 | 0.00 | 0.00 | +0.00 |
| Fix Quality | 0.00 | 0.00 | 0.00 | +0.00 |
| Investigation Methodology | 0.00 | 0.00 | 0.00 | +0.00 |
| **Composite** | **0.560** | **0.367** | **0.535** | **-0.025** |

## Analysis Details

### Baseline (bare: 0.560, wolfden: 0.367)

**Summary:** This task required implementing wc_linux_kernel_rng_is_wolfcrypt() — a new WOLFSSL_API function that takes a struct crypto_rng pointer and checks whether it belongs to wolfcrypt's LKCAPI DRBG registration. It also required removing an incorrect crypto_put_default_rng() call, adding a WC_NO_CONSTRUCTORS build gate to rng_bank, and critically relocating the LINUXKM_LKCAPI_REGISTER_HASH_DRBG_DEFAULT macro definition earlier in linuxkm_wc_port.h to prevent get_random_bytes() recursion.

Bare Claude correctly identified 3 of 4 change areas: the new function (wrong signature — used global instead of parameter), the crypto_put_default_rng removal (correct reasoning), and the WC_NO_CONSTRUCTORS gate (correct). It missed the macro relocation entirely.

wolfDen Claude performed worse despite having domain knowledge loaded. It misunderstood the primary task, describing a NULL-safety snapshot fix instead of implementing wc_linux_kernel_rng_is_wolfcrypt(). It correctly validated the crypto_put_default_rng removal and WC_NO_CONSTRUCTORS gate but treated the task as a review rather than an implementation. It also missed the macro relocation.

Neither Claude understood the linuxkm_wc_port.h file's zone structure — that defines placed before BUILDING_WOLFSSL have different visibility than those inside it, and that LINUXKM_LKCAPI_REGISTER_HASH_DRBG_DEFAULT must be visible to random.c. Neither understood the API design pattern where kernel-facing wolfSSL functions take explicit handles (struct crypto_rng *rng) rather than accessing globals. The key systemic gap is that Linux kernel module LKCAPI integration patterns — macro visibility zones, API handle conventions, and recursion prevention architecture — are not captured in wolfDen's knowledge base.

### Iteration 1 (bare: 0.497, wolfden: 0.535)

**Summary:** This PR required four changes: (1) adding wc_linux_kernel_rng_is_wolfcrypt() as a WOLFSSL_API function taking struct crypto_rng *rng, (2) removing an incorrect crypto_put_default_rng() call that created a ref-count underflow, (3) adding WC_NO_CONSTRUCTORS guard to rng_bank, and (4) relocating the LINUXKM_LKCAPI_REGISTER_HASH_DRBG_DEFAULT macro definition from inside BUILDING_WOLFSSL to before it to ensure visibility for random.c recursion prevention.

Bare Claude correctly identified changes 2-3 and attempted change 1 but got the function signature wrong — using globals instead of the struct crypto_rng *rng parameter pattern. It missed the macro relocation entirely.

wolfDen Claude correctly analyzed changes 2-3 and gave a conceptually accurate description of change 1, but treated the task as a review rather than implementation and also missed the macro relocation. Its ref-count underflow analysis was more precise.

Neither Claude understood linuxkm_wc_port.h's zone architecture — that defines placed before vs inside BUILDING_WOLFSSL have different visibility to consuming translation units, and that LINUXKM_LKCAPI_REGISTER_HASH_DRBG_DEFAULT must be visible to random.c to gate out get_random_bytes() calls. Neither understood the kernel-facing API convention of taking explicit handle parameters.

The key systemic gaps are: (1) linuxkm_wc_port.h's macro visibility zone structure, (2) the convention that LKCAPI functions take explicit kernel struct handles, and (3) the WOLFSSL_API symbol export pattern for kernel module declarations with forward-declared structs. These affect any PR touching linuxkm LKCAPI integration.

**Changes applied:**
- `scaffold/.claude/rules/platforms/linux.md` — The linuxkm section was a 5-line stub. Any developer working on Linux kernel module code needs to understand (1) that linuxkm_wc_port.h has visibility zones and moving macros between them has semantic
- `scaffold/.claude/rules/platforms/linux.md` — The macro gate chain pattern is reusable for any developer trying to understand or modify which LKCAPI algorithms get registered. It's a non-obvious pattern where multiple suppression macros can preve

