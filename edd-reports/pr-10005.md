# Eval Report: PR #10005

**Repo:** wolfssl/wolfssl
**PR Title:** Fix sz==0 buffer underflow in devcrypto AES-CBC
**Problem:** Fix sz==0 buffer underflow in devcrypto AES-CBC (source: pr_body)
**Generated:** 2026-03-22T14:11:55Z
**Verdict:** regressed

## Scores

| Metric | Bare | wolfDen | wolfDen (iter 1) | wolfDen Delta |
| --- | --- | --- | --- | --- |
| Root Cause Identification | 0.00 | 0.00 | 0.00 | +0.00 |
| File Targeting | 0.00 | 0.00 | 0.00 | +0.00 |
| Knowledge Application | 0.00 | 0.00 | 0.00 | +0.00 |
| Fix Quality | 0.00 | 0.00 | 0.00 | +0.00 |
| Investigation Methodology | 0.00 | 0.00 | 0.00 | +0.00 |
| **Composite** | **0.688** | **0.420** | **0.379** | **-0.308** |

## Analysis Details

### Baseline (bare: 0.688, wolfden: 0.420)

**Summary:** The task was a straightforward buffer underflow fix in wolfSSL's devcrypto AES-CBC backend: when sz==0, pointer arithmetic like `out + sz - AES_BLOCK_SIZE` underflows. The correct fix (matching the engineer's PR) is minimal — add `if (sz == 0) return 0;` after existing truncation/validation logic in both encrypt and decrypt functions, preserving all existing behavior.

Bare Claude correctly identified the bug mechanism, proposed exactly the right fix (early return 0 after truncation in encrypt, after validation in decrypt), and kept the scope appropriately minimal. Its description maps almost perfectly to the ground truth diff.

wolfDen Claude identified the same underlying bug but over-applied domain knowledge about wolfSSL's 'canonical patterns.' It removed the sz rounding in encrypt (which the ground truth keeps), restructured guard ordering to match aes.c's software path, and added WOLFSSL_AES_CBC_LENGTH_CHECKS ifdef support — none of which appear in the actual PR. The rounding removal is the most damaging: it changes the function's contract from 'silently round down' to 'reject non-aligned input,' which could break callers.

This is a case where domain knowledge was counterproductive. wolfDen's awareness of how the main aes.c software implementation structures its guards led it to 'normalize' a port backend that intentionally differs. The key systemic gap is that wolfDen's knowledge doesn't distinguish between the software crypto path and hardware/kernel port backends — these backends (devcrypto, afalg, etc.) may intentionally have different contracts. Future knowledge should document that port backends in `wolfcrypt/src/port/` should be treated as having their own conventions, and fixes should be minimal unless there's an explicit request to align them with the canonical implementation.

### Iteration 1 (bare: 0.731, wolfden: 0.379)

**Summary:** This was a minimal buffer underflow fix in wolfSSL's devcrypto AES-CBC port backend. When sz==0, pointer arithmetic like `out + sz - AES_BLOCK_SIZE` underflows before the buffer. The correct fix adds `if (sz == 0) return 0;` after existing truncation (encrypt) and validation (decrypt) logic — three lines per function, preserving all existing behavior including the encrypt function's intentional rounding of sz to block alignment.

Bare Claude nailed it: correctly identified the underflow mechanism in both functions, proposed exactly the right minimal fix (early return 0 after truncation/validation), and kept scope appropriately tight. Its description maps almost perfectly to the ground truth diff.

wolfDen Claude identified the same root cause but then over-applied domain knowledge. It removed the sz rounding in encrypt (which the ground truth keeps), restructured guard ordering to match aes.c's software path, and added WOLFSSL_AES_CBC_LENGTH_CHECKS ifdef support — none of which appear in the actual PR. The rounding removal is the most damaging: it changes the function's contract from 'silently round down' to 'reject non-aligned input,' which could break callers.

The key systemic gap is that wolfDen's knowledge doesn't distinguish between the software crypto path (wolfcrypt/src/aes.c) and hardware/kernel port backends (wolfcrypt/src/port/). These backends intentionally have different contracts — devcrypto's silent rounding is a deliberate design choice, not a deviation to correct. wolfDen needs explicit guidance that port backends should be treated as having their own conventions, and that fixes to port code should be minimal unless there's an explicit request to align with the canonical implementation. Domain knowledge about 'how wolfSSL structures its guards' became a liability when applied to a subsystem that follows different rules.

**Changes applied:**
- `scaffold/.claude/rules/discovered-patterns.md` — The core failure was wolfDen normalizing a port backend to match aes.c's canonical pattern, breaking the backend's intentional contract. This pattern applies to any future bug fix or enhancement in an

