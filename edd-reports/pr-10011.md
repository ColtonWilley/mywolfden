# Eval Report: PR #10011

**Repo:** wolfssl/wolfssl
**PR Title:** Don't force enable MD5 with --enable-jni
**Problem:** Don't force enable MD5 with --enable-jni (source: pr_body)
**Generated:** 2026-03-22T13:26:05Z
**Verdict:** regressed

## Scores

| Metric | Bare | wolfDen | wolfDen (iter 1) | wolfDen Delta |
| --- | --- | --- | --- | --- |
| Root Cause Identification | 0.00 | 0.00 | 0.00 | +0.00 |
| File Targeting | 0.00 | 0.00 | 0.00 | +0.00 |
| Knowledge Application | 0.00 | 0.00 | 0.00 | +0.00 |
| Fix Quality | 0.00 | 0.00 | 0.00 | +0.00 |
| Investigation Methodology | 0.00 | 0.00 | 0.00 | +0.00 |
| **Composite** | **0.482** | **0.823** | **0.573** | **+0.090** |

## Analysis Details

### Baseline (bare: 0.482, wolfden: 0.823)

**Summary:** The task was simple: remove one line (`ENABLED_MD5="yes"`) from the `--enable-jni` block in `configure.ac` so that JNI no longer force-enables MD5 when enabling OPENSSLEXTRA, allowing FIPS builds to keep MD5 disabled.

Bare Claude proposed a conditional guard — checking `$enable_md5` before force-enabling MD5 — which preserves the old behavior by default and only skips MD5 when the user explicitly passes `--disable-md5`. This is functionally different from the ground truth: the engineer decided MD5 should simply never be force-enabled by JNI, period. Bare Claude's approach is over-conservative because it misunderstood the relationship between OPENSSLEXTRA and MD5, treating the force-enable as intentional rather than accidental coupling.

wolfDen Claude correctly identified that the fix is to remove the `ENABLED_MD5="yes"` line entirely, matching the ground truth exactly. It reasoned that OPENSSLEXTRA doesn't inherently require MD5 and that the general MD5 dependency logic elsewhere in configure.ac handles legitimate needs. It also correctly identified the FIPS implications and referenced the upstream PR.

The key domain knowledge gap is understanding wolfSSL's configure.ac dependency philosophy: when a feature like JNI enables OPENSSLEXTRA, it should enable only OPENSSLEXTRA — not cascade to OPENSSLEXTRA's optional sub-dependencies like MD5. wolfDen's FIPS boundary knowledge and understanding of the build system's layered enable logic led it to the correct minimalist fix, while bare Claude's generic autoconf knowledge led to an over-engineered conditional that preserves problematic coupling.

### Iteration 1 (bare: 0.352, wolfden: 0.573)

**Summary:** The task required removing a single line (`ENABLED_MD5="yes"`) from the JNI block in configure.ac (~line 7980) where enabling OPENSSLEXTRA for JNI unnecessarily force-enabled MD5, breaking FIPS builds that need MD5 disabled.

Bare Claude proposed a conditional guard checking `$enable_md5` before force-enabling — functionally different from the ground truth. This approach treats the MD5 force-enable as intentional behavior that should only be overridden by explicit user action, rather than recognizing it as accidental coupling. The autoconf variable naming knowledge (`$enable_md5` vs `$ENABLED_MD5`) is correct but misapplied.

wolfDen Claude correctly identified the root cause (transitive dependency: JNI→OPENSSLEXTRA→MD5) and the right principle (integration flags shouldn't cascade to non-certified algorithms). However, it targeted the wrong location — lines 6042-6053 (general MD5 dependency conditions) instead of ~line 7980 (JNI-specific block). This broader fix would remove OPENSSLEXTRA→MD5 coupling globally rather than surgically in the JNI path, potentially affecting other features.

wolfDen's advantage was clear in reasoning quality: FIPS boundary awareness, dependency philosophy, and knowledge that source guards (#ifndef NO_MD5) make the removal safe. Bare Claude lacked this context entirely. However, wolfDen's domain knowledge was too abstract to pinpoint the exact remediation point in configure.ac's complex structure. The key gap is mapping configure.ac's multiple MD5-enabling locations and understanding which one to target for a given feature path. wolfDen needs knowledge about configure.ac's feature block structure — specifically that JNI, haproxy, nginx etc. each have their own blocks that independently force-enable dependencies, and surgical fixes should target the specific feature block rather than the general dependency resolution section.

**Changes applied:**
- `scaffold/.claude/rules/build-system.md` — This is a general configure.ac design principle that applies to every integration flag block. Bare Claude added a conditional guard instead of removing the unnecessary force-enable because it didn't u
- `scaffold/.claude/rules/build-system.md` — This constraint applies broadly — any developer adding or modifying configure.ac flag blocks near FIPS-sensitive algorithms needs to understand that force-enabling non-certified algorithms breaks FIPS

