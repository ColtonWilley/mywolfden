# Eval Report: PR #9982

**Repo:** wolfssl/wolfssl
**PR Title:** DoTls13CertificateRequest: call CertSetupCbWrapper only once
**Problem:** DoTls13CertificateRequest: call CertSetupCbWrapper only once (source: pr_body)
**Generated:** 2026-03-22T21:00:30Z
**Verdict:** neutral

## Verdicts

| Pillar | Bare | wolfDen | wolfDen (iter 1) |
| --- | --- | --- | --- |
| **Overall** | **pass** | **fail** | **fail** |

## Analysis Details

### Baseline (bare: pass, wolfden: fail)

**Summary:** The task required understanding a duplicate function call bug in DoTls13CertificateRequest where CertSetupCbWrapper was invoked twice when OPENSSL_EXTRA was defined — once under #ifdef WOLFSSL_CERT_SETUP_CB before TLSX_Parse, and once under #ifdef OPENSSL_EXTRA after it. Since OPENSSL_EXTRA auto-enables WOLFSSL_CERT_SETUP_CB in settings.h, both guards are true simultaneously. The fix removes the early call and changes the remaining call's guard to WOLFSSL_CERT_SETUP_CB, matching the TLS 1.2 analog pattern.

Bare Claude nailed this completely. It correctly identified both call sites, explained the OPENSSL_EXTRA → WOLFSSL_CERT_SETUP_CB auto-enable relationship, identified both changes (remove early call, change guard), and referenced the TLS 1.2 analog as justification. Every technical claim aligns with the actual PR diff.

wolfDen Claude failed on both pillars. It described a fundamentally different fix — removing a '|| ssl->ctx->certSetupCb != NULL' condition from a branch check — that does not appear anywhere in the PR diff. It claimed the problem was about a condition forcing entry into a cert-present branch without a loaded certificate, rather than the actual problem of duplicate callback invocation. The response never addresses the core issue named in the PR title: calling CertSetupCbWrapper only once. This is a fabricated description of both the problem and the fix.

wolfDen's domain knowledge provided no observable advantage here. The task required reading specific code in tls13.c, understanding preprocessor guard relationships in settings.h, and recognizing the TLS 1.2 analog pattern — all of which bare Claude accomplished through direct code reading. wolfDen's loaded knowledge about wolfSSL patterns did not prevent it from misidentifying the fix entirely.

The systemic gap is in wolfSSL's preprocessor guard hierarchy — specifically how OPENSSL_EXTRA auto-enables subordinate feature guards like WOLFSSL_CERT_SETUP_CB, creating situations where code wrapped in different guards unexpectedly both activate. This interaction pattern between feature flags is a common source of bugs in wolfSSL and would benefit from explicit documentation in domain knowledge.

### Iteration 1 (bare: pass, wolfden: fail)

**Summary:** This task required identifying a duplicate function call bug in DoTls13CertificateRequest where CertSetupCbWrapper was invoked twice when OPENSSL_EXTRA was defined. The root cause is wolfSSL's preprocessor guard hierarchy: OPENSSL_EXTRA auto-enables WOLFSSL_CERT_SETUP_CB in settings.h, so an early call guarded by #ifdef WOLFSSL_CERT_SETUP_CB and a later call guarded by #ifdef OPENSSL_EXTRA both activate simultaneously. The fix is minimal — remove the early call (5 lines) and change the later call's guard from OPENSSL_EXTRA to WOLFSSL_CERT_SETUP_CB (1 line change).

Bare Claude nailed every aspect. It correctly identified both call sites with approximate line numbers, explained the OPENSSL_EXTRA → WOLFSSL_CERT_SETUP_CB auto-enable mechanism, described both required changes (remove early call, change guard), and referenced the TLS 1.2 analog in internal.c as justification for the 'call after parsing' placement. Every technical claim aligns with the actual PR diff.

wolfDen Claude failed catastrophically by fabricating an entirely different fix. It described a consolidation of CBClientCert logic from SendTls13Certificate into DoTls13CertificateRequest, including wolfSSL_use_certificate/wolfSSL_use_PrivateKey calls, WOLFSSL_ERROR_WANT_X509_LOOKUP handling, and removal of x509/pkey variables from SendTls13Certificate. None of these changes appear in the PR diff. The PR does not touch SendTls13Certificate at all. wolfDen never identified the actual problem (duplicate CertSetupCbWrapper invocation due to overlapping preprocessor guards) or the actual fix (removing one call and correcting one guard).

This is the second consecutive iteration where wolfDen fails on this task, though with a different fabrication than iteration 0. The consistent failure pattern suggests wolfDen's domain knowledge creates false confidence — it has enough context about wolfSSL's callback architecture to construct plausible-sounding but incorrect narratives about cert callback consolidation, rather than performing the straightforward code reading that bare Claude did successfully.

The key systemic gap is wolfSSL's preprocessor guard hierarchy. Guards like OPENSSL_EXTRA, WOLFSSL_CERT_SETUP_CB, OPENSSL_ALL, and others form an implicit dependency tree where enabling one flag auto-enables others via settings.h. This creates subtle bugs where code protected by different guards unexpectedly both activates. Domain knowledge should document these auto-enable relationships and flag them as a common bug pattern, particularly in certificate handling and callback code where multiple independent feature flags gate overlapping functionality.

**Changes applied:**
- `scaffold/.claude/rules/discovered-patterns.md` — The core failure was not recognizing that two differently-guarded blocks both called CertSetupCbWrapper because OPENSSL_EXTRA auto-enables WOLFSSL_CERT_SETUP_CB. This is a general bug pattern in wolfS
- `scaffold/.claude/rules/build-system.md` — The existing settings.h section only covers platform-to-feature auto-enables. Feature-to-feature cascading (OPENSSL_EXTRA enabling WOLFSSL_CERT_SETUP_CB) is the same mechanism but a different use case

