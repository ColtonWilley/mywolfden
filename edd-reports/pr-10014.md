# Eval Report: PR #10014

**Repo:** wolfssl/wolfssl
**PR Title:** Add missing include in asn.h
**Problem:** Add missing include in asn.h (source: pr_body)
**Generated:** 2026-03-22T13:05:05Z
**Verdict:** regressed

## Scores

| Metric | Bare | wolfDen | wolfDen (iter 1) | wolfDen Delta |
| --- | --- | --- | --- | --- |
| Root Cause Identification | 0.00 | 0.00 | 0.00 | +0.00 |
| File Targeting | 0.00 | 0.00 | 0.00 | +0.00 |
| Knowledge Application | 0.00 | 0.00 | 0.00 | +0.00 |
| Fix Quality | 0.00 | 0.00 | 0.00 | +0.00 |
| Investigation Methodology | 0.00 | 0.00 | 0.00 | +0.00 |
| **Composite** | **0.887** | **0.912** | **0.843** | **-0.045** |

## Analysis Details

### Baseline (bare: 0.887, wolfden: 0.912)

**Summary:** This task required adding a missing conditional #include for sha512.h in wolfssl/wolfcrypt/asn.h, guarded by WOLFSSL_SHA384 || WOLFSSL_SHA512. The fix is 3 lines inserted after the existing sha256.h include, following the identical pattern used by neighboring hash header includes (sha.h, md5.h, sm3.h). Both Claude sessions produced the exact correct fix matching the ground truth PR. Bare Claude identified the correct location (line 86), the correct guard conditions, the root cause (transitive include order dependency), and the regression source (#9761). wolfDen Claude found the fix already present as an uncommitted change in the working tree and confirmed it matched a commit on another branch, adding context about the OCSP responder origin of #9761 and noting the guard mirrors sha512.h's own conditions. The scores are nearly identical because the task requires no deep domain knowledge — it is a straightforward missing-include bug that any C developer familiar with conditional compilation can diagnose and fix by reading the surrounding include block. wolfDen's loaded knowledge about wolfSSL's build system, ifdef conventions, and header structure provided no measurable advantage. This task is not a good discriminator for evaluating domain knowledge value. To test wolfDen's worth, tasks should require understanding of cross-cutting wolfSSL concerns like FIPS boundaries, API layer separation, cryptoCb dispatch, or platform-specific compilation paths — areas where reading local code alone is insufficient.

### Iteration 1 (bare: 0.818, wolfden: 0.843)

**Summary:** This task required adding a 3-line conditional #include for sha512.h in wolfssl/wolfcrypt/asn.h, guarded by WOLFSSL_SHA384 || WOLFSSL_SHA512, placed between the existing sha256.h and sm3.h includes. The fix addresses a build breakage where AsnHashes struct references WC_SHA384_DIGEST_SIZE and WC_SHA512_DIGEST_SIZE without directly including their defining header. Both Claude sessions produced the exact correct fix matching the ground truth PR. Bare Claude identified the correct insertion point (line 86), correct guard conditions, the pattern of neighboring includes, and the regression source (#9761). wolfDen Claude found the fix already present as an uncommitted local change and performed additional verification: checking sha512.h's own internal guard (line 32), confirming no circular include risk, and referencing wolfDen's 'discovered-patterns' rule about conditional include guards. The extra rigor is marginally valuable but did not change the outcome. This remains a poor discriminator for domain knowledge value. The task requires only: (1) finding where the undefined symbols are defined, (2) reading the surrounding include block to see the pattern, (3) adding a matching conditional include. All information is local to the file. To meaningfully test wolfDen, tasks should involve cross-file architectural decisions where the correct approach isn't visible from reading a single file — such as adding a new algorithm to the build system, implementing a new TLS extension, or modifying FIPS-boundary code where the consequences span multiple subsystems.

**Changes applied:**
- `scaffold/.claude/rules/discovered-patterns.md` — wolfSSL's conditional include convention is non-obvious — the guard must match the target header's internal gate, not just the feature using the type. This applies to any PR adding new type dependenci

