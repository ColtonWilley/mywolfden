# Eval Report: PR #10010

**Repo:** wolfssl/wolfssl
**PR Title:** Dilithium: fix API so that context length is byte
**Problem:** Dilithium: fix API so that context length is byte (source: pr_body)
**Generated:** 2026-03-22T13:48:57Z
**Verdict:** neutral

## Scores

| Metric | Bare | wolfDen | wolfDen (iter 1) | wolfDen Delta |
| --- | --- | --- | --- | --- |
| Root Cause Identification | 0.00 | 0.00 | 0.00 | +0.00 |
| File Targeting | 0.00 | 0.00 | 0.00 | +0.00 |
| Knowledge Application | 0.00 | 0.00 | 0.00 | +0.00 |
| Fix Quality | 0.00 | 0.00 | 0.00 | +0.00 |
| Investigation Methodology | 0.00 | 0.00 | 0.00 | +0.00 |
| **Composite** | **0.660** | **0.787** | **0.772** | **+0.112** |

## Analysis Details

### Baseline (bare: 0.660, wolfden: 0.787)

**Summary:** The task was a straightforward API consistency fix: Dilithium verify functions used `word32` for the context length parameter while sign functions already correctly used `byte`. Per FIPS 204, context length is a single byte (0..255), so all four functions (two internal, two public) needed their signatures changed, two unnecessary `(byte)` casts removed, and the Rust FFI wrapper updated to cast `as u8` instead of `as u32`.

Both Claudes correctly identified the core C changes: the four function signatures in dilithium.c, the two header declarations in dilithium.h, and the two cast removals. Their analyses were accurate and well-reasoned, correctly noting the sign/verify asymmetry and the FIPS 204 constraint.

The meaningful difference was completeness across language boundaries. wolfDen Claude found the Rust wrapper and identified the two `as u32` → `as u8` changes needed there. Bare Claude stopped at the C layer. This matters because leaving the Rust wrapper unchanged would cause a type mismatch at the FFI boundary — the Rust code would pass a u32 where the C function now expects a byte.

This is a moderate wolfDen advantage. The task itself was simple enough that both Claudes handled the core C changes identically, but wolfDen's awareness of wolfSSL's multi-language wrapper architecture led it to check beyond the primary implementation language. The gap suggests that domain knowledge about wolfSSL's wrapper ecosystem (Rust, JNI, Python) and a systematic checklist for API signature changes would help ensure cross-language consistency on similar tasks.

### Iteration 1 (bare: 0.708, wolfden: 0.772)

**Summary:** This was a straightforward API consistency fix: Dilithium verify functions used word32 for context length while sign functions already used byte. Per FIPS 204/ML-DSA, context length is 0..255 bytes, so byte is correct. The PR changed 4 function signatures in dilithium.c (2 static, 2 public), 2 declarations in dilithium.h, removed 2 now-unnecessary (byte) casts, and updated 2 Rust FFI casts from 'as u32' to 'as u8'.

Both Claudes correctly identified all C-layer changes: the sign/verify type asymmetry, the four functions needing word32→byte, the header declarations, and the cast removals. Their reasoning about the FIPS 204 constraint was accurate.

The meaningful gap was cross-language completeness. wolfDen Claude found the Rust wrapper changes; bare Claude did not. This matters because leaving Rust casting as u32 when the C function now expects byte creates an FFI type mismatch. wolfDen's advantage came specifically from awareness that wolfSSL maintains language wrappers that must stay synchronized with C API changes.

However, wolfDen's evidence discipline was slightly weaker — it claimed the fix was 'already applied' and cited a specific commit hash, which is questionable given the pre-PR evaluation context. Domain knowledge should enhance verification, not replace it.

The systemic gap is wrapper-layer propagation: any wolfSSL API signature change should trigger a checklist covering wrapper/rust/, wrapper/python/, and JNI bindings. This pattern recurs for any public API modification.

**Changes applied:**
- `scaffold/.claude/rules/coding-standards.md` — This is the exact pattern that differentiated wolfDen from bare Claude on this PR, and it generalizes to ANY public API change across wolfSSL — not just Dilithium. Any developer changing a C function 

