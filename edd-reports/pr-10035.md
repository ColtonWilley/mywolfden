# Eval Report: PR #10035

**Repo:** wolfssl/wolfssl
**PR Title:** Allow zero-length input in _wc_Hash_Grow and fix SHA Copy MAX32666
**Problem:** Allow zero-length input in _wc_Hash_Grow and fix SHA Copy MAX32666 (source: pr_body)
**Generated:** 2026-03-22T10:50:23Z
**Verdict:** regressed

## Scores

| Metric | Bare | wolfDen | wolfDen (iter 1) | wolfDen Delta |
| --- | --- | --- | --- | --- |
| Root Cause Identification | 0.00 | 0.00 | 0.00 | +0.00 |
| File Targeting | 0.00 | 0.00 | 0.00 | +0.00 |
| Knowledge Application | 0.00 | 0.00 | 0.00 | +0.00 |
| Fix Quality | 0.00 | 0.00 | 0.00 | +0.00 |
| Investigation Methodology | 0.00 | 0.00 | 0.00 | +0.00 |
| **Composite** | **0.662** | **0.703** | **0.662** | **+0.000** |

## Analysis Details

### Baseline (bare: 0.662, wolfden: 0.703)

**Summary:** This task involved three targeted fixes: (1) allowing zero-length input in _wc_Hash_Grow as a no-op, (2) reverting a broken reordering of the MAX32666 SHA deep-copy sequence that created a dangling pointer, and (3) adding WOLFSSL_HASH_KEEP to CI. The PR description was unusually detailed, essentially specifying exactly what to change and why.

Both Claudes correctly identified all three changes and understood the high-level mechanics. The hash.c fix was straightforward — splitting `inSz <= 0` into separate negative and zero checks. The CI addition was trivial. The MAX32666 fix was the most complex, involving a shallow struct copy that aliases pointers through dstMsg.

Both Claudes described the max3266x.c fix as 'moving srcBuf assignment after XMEMCPY' but missed that the ground truth went further: it changed the condition from `if (srcBuf != NULL)` to `if (*dstMsg != NULL)` and moved srcBuf assignment inside the conditional, plus initialized srcBuf to NULL and added a detailed defensive comment. This restructuring makes the logic more robust and self-documenting.

wolfDen's advantage was marginal — slightly better temporal reasoning about the dangling pointer sequence. The detailed PR description reduced the need for domain knowledge since both Claudes were essentially told exactly what was wrong and what to fix. This task primarily tested code comprehension rather than domain navigation. The systemic gap is in hardware port copy semantics documentation — the relationship between struct-level XMEMCPY, pointer aliasing through output parameters, and heap ownership in Maxim TPU ports is non-obvious and would benefit from wolfDen coverage for future MAX32666 work.

### Iteration 1 (bare: 0.623, wolfden: 0.662)

**Summary:** This task involved three fixes: (1) allowing zero-length input in _wc_Hash_Grow, (2) reverting a broken reordering of MAX32666 SHA deep-copy logic, and (3) adding WOLFSSL_HASH_KEEP to CI. The PR description was highly detailed, essentially specifying the exact problem and solution for each fix, reducing the need for independent domain reasoning.

Both Claudes correctly identified all three changes and understood the core mechanics. The hash.c fix (splitting inSz <= 0 into separate negative/zero checks) and CI addition were handled identically and correctly by both. The MAX32666 fix was the differentiator — both described it as 'moving srcBuf assignment after XMEMCPY' but missed the ground truth's more thorough restructuring: changing the condition from `if (srcBuf != NULL)` to `if (*dstMsg != NULL)`, moving srcBuf inside the conditional, NULL-initializing srcBuf, and adding a detailed defensive comment. Functionally, both approaches would work since srcBuf is set from *dstMsg immediately before the check, but the ground truth's version is more robust and self-documenting.

wolfDen's advantage was modest: it traced the breaking commit by hash (30cb25e49 'Fixes from AI review'), providing better provenance for the bug. However, this didn't produce a more accurate implementation. The systemic gap is in hardware port copy semantics — the relationship between struct-level XMEMCPY, pointer aliasing through output parameters, heap ownership across src/dst, and defensive coding patterns for embedded platforms. This knowledge would benefit future MAX32666 and similar hardware port work where shallow-copy-then-deep-copy patterns are common.

**Changes applied:**
- `scaffold/.claude/rules/discovered-patterns.md` — This pattern applies to every hardware port Copy function across Maxim, STM32, Renesas, and other accelerator backends. The specific failure mode — saving a pointer before shallow copy, then using the

