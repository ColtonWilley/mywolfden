# Eval Report: PR #10003

**Repo:** wolfssl/wolfssl
**PR Title:** Fixes from AI review
**Problem:** Fixes from AI review (source: pr_body)
**Generated:** 2026-03-22T14:20:07Z
**Verdict:** neutral

## Scores

| Metric | Bare | wolfDen | wolfDen (iter 1) | wolfDen Delta |
| --- | --- | --- | --- | --- |
| Root Cause Identification | 0.00 | 0.00 | 0.00 | +0.00 |
| File Targeting | 0.00 | 0.00 | 0.00 | +0.00 |
| Knowledge Application | 0.00 | 0.00 | 0.00 | +0.00 |
| Fix Quality | 0.00 | 0.00 | 0.00 | +0.00 |
| Investigation Methodology | 0.00 | 0.00 | 0.00 | +0.00 |
| **Composite** | **0.045** | **0.015** | **0.006** | **-0.039** |

## Analysis Details

### Baseline (bare: 0.045, wolfden: 0.015)

**Summary:** The PR 'Fixes from AI review' is a bulk bug-fix PR touching 50+ files across wolfcrypt/src/port/, fixing real defects found by AI-assisted code review: hardware crypto lock leaks on early-return error paths (Espressif esp_aes_hw_Leave, esp_mp_hw_unlock; Renesas tsip_hw_unlock), type mismatches (renesas_fspsm_rsa.c signature.data_length cast), wrong constant references (SHA512_256 using SHA512_224 digest size, sizeof(buffer) vs sizeof(fspbuf)), socket descriptor comparison bugs (AF_ALG using <= 0 instead of == WC_SOCK_NOTSET), missing error returns, off-by-one date handling, buffer overflow protection, and zero-length allocation guards.

Bare Claude asked for clarification — a reasonable response to a vague prompt, though it could have searched git history to find context. wolfDen Claude performed worse: it confidently but incorrectly identified the task as formatting fixes in its own .claude/rules/coding-standards.md file, never searching any C source code. This is the worst failure mode — domain knowledge creating false confidence rather than genuine understanding.

The key systemic gap is twofold. First, neither Claude had an investigation methodology for disambiguating vague prompts (searching git log, recent PRs, or scanning the port directory). Second, the cross-cutting pattern of hardware resource lock/unlock management on error paths — which appears identically across Espressif, Renesas, TSIP, AF_ALG, and other port backends — is not documented. This pattern (acquire lock → operation → release on ALL paths including errors) is the single most common bug class in this PR and would benefit from explicit documentation as a port-layer coding requirement.

### Iteration 1 (bare: 0.043, wolfden: 0.006)

**Summary:** PR #10003 is a bulk bug-fix touching 50+ files across wolfcrypt/src/port/, fixing defects found by AI-assisted code review. The dominant bug class is hardware crypto resource lock leaks on early-return error paths (Espressif esp_aes_hw_InUse/Leave, esp_mp_hw_unlock; Renesas tsip_hw_unlock), appearing identically across multiple port backends. Secondary fixes include type mismatches (Renesas RSA signature.data_length cast, SHA512_256 using wrong digest size constant, sizeof wrong variable), socket descriptor comparison bugs (AF_ALG using <= 0 instead of == WC_SOCK_NOTSET), missing error returns, off-by-one date handling, buffer overflow protection, and zero-length allocation guards.

Bare Claude asked for clarification — a reasonable response to a vague prompt that at least acknowledged uncertainty, though it could have searched git history for context. wolfDen Claude performed worse: it confidently but incorrectly identified the task as fixing broken cross-references in its own .claude/rules/ scaffold files, never searching any C source code. This is the worst failure mode — domain knowledge creating false confidence rather than genuine understanding. The wolfDen Claude's knowledge about Espressif and Renesas platforms didn't help because it never looked at actual port code.

The key systemic gaps are: (1) no investigation methodology for disambiguating vague prompts — searching git log, recent PRs, or scanning directories should be a default step; (2) the hardware resource lock/unlock lifecycle pattern (acquire lock → operation → release on ALL paths including errors) is the single most common bug class in port code and needs explicit documentation; (3) platform-specific API contract pitfalls (WC_SOCK_NOTSET comparisons, Renesas type conventions) are undocumented despite being recurring defect sources.

**Changes applied:**
- `scaffold/.claude/rules/discovered-patterns.md` — This is the single most common bug class across port backends — it appeared identically in Espressif, Renesas, AF_ALG, and other backends in this PR alone. Any developer fixing bugs or adding features
- `scaffold/.claude/rules/discovered-patterns.md` — fd==0 bugs are subtle and recur whenever developers touch socket-based port backends. The pattern applies to AF_ALG, devcrypto, and any future kernel-interface port. Documenting the sentinel conventio

