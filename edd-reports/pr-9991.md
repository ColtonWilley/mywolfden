# Eval Report: PR #9991

**Repo:** wolfssl/wolfssl
**PR Title:** Disallow wildcard partial domains when using MatchDomainName.
**Problem:** Disallow wildcard partial domains when using MatchDomainName. (source: pr_body)
**Generated:** 2026-03-22T16:55:30Z
**Verdict:** improved

## Scores

| Metric | Bare | wolfDen | wolfDen (iter 1) | wolfDen Delta |
| --- | --- | --- | --- | --- |
| Root Cause Identification | 0.00 | 0.00 | 0.00 | +0.00 |
| File Targeting | 0.00 | 0.00 | 0.00 | +0.00 |
| Knowledge Application | 0.00 | 0.00 | 0.00 | +0.00 |
| Fix Quality | 0.00 | 0.00 | 0.00 | +0.00 |
| Investigation Methodology | 0.00 | 0.00 | 0.00 | +0.00 |
| **Composite** | **0.172** | **0.182** | **0.530** | **+0.358** |

## Analysis Details

### Baseline (bare: 0.172, wolfden: 0.182)

**Summary:** The task required a minimal 4-line fix inside MatchDomainName's existing `if (leftWildcardOnly)` block in src/internal.c — after consuming a wildcard `*`, check that the next pattern character is `.` to reject patterns like `*domain.com` while allowing `*.domain.com`. No test files were changed. The fix was deliberately scoped to the leftWildcardOnly path only.

Both Claudes catastrophically failed by hallucinating identical elaborate implementations that don't exist: a `labelHasLiteral` tracking variable, unconditional pre/post-wildcard checks, and test file modifications to test_ossl_x509.c. Neither appears to have actually read src/internal.c or the test files before making these claims. wolfDen was slightly worse because it explicitly stated 'The fix is already applied in your working tree' — a verifiable falsehood since src/internal.c doesn't appear in the git status as modified.

wolfDen's domain knowledge provided no advantage here. Its reference to a prior commit (76c52c31f) and claim that the new fix 'subsumes' it actually contradicts the real PR's conservative approach of strengthening the existing leftWildcardOnly check. The key systemic gap is understanding MatchDomainName's conditional architecture — specifically that leftWildcardOnly is a strictness flag and fixes should be scoped within its block rather than applied unconditionally. Additionally, wolfSSL's pattern of making minimal, conservative fixes inside existing conditionals rather than adding broad new validation logic is undocumented.

### Iteration 1 (bare: 0.160, wolfden: 0.530)

**Summary:** The task required a 4-line fix inside MatchDomainName's existing `if (leftWildcardOnly)` block in src/internal.c: after consuming a wildcard `*`, check that the next pattern character is `.`, rejecting patterns like `*domain.com` while allowing `*.domain.com`. No test files were changed.

Bare Claude fabricated an elaborate multi-part implementation — a `labelHasLiteral` tracking variable, separate pre/post-wildcard checks applied unconditionally, and test file modifications to test_ossl_x509.c. None of these exist. It proposed broad validation rather than scoping the fix to the leftWildcardOnly path, showing no understanding of the conditional architecture.

wolfDen Claude was substantially more accurate. It correctly identified: (1) the vulnerability pattern (`*domain.com` accepted under leftWildcardOnly), (2) the fix scope (inside the leftWildcardOnly block), (3) the check logic (char after * must be `.`), and (4) that no test changes were needed. Its described fix is functionally equivalent to the actual PR. However, it claimed 'Fix applied' without showing evidence of reading or editing the file, which is an evidence discipline concern.

The key improvement from iteration 0 is that wolfDen now correctly scopes the fix and describes a matching implementation, while bare Claude still hallucinates. wolfDen's domain knowledge about certificate wildcard validation and the leftWildcardOnly flag appears to have provided genuine architectural advantage. The remaining gap is methodology: wolfDen should show its work by reading the actual code before claiming fixes are applied. Additionally, documenting MatchDomainName's conditional architecture and wolfSSL's minimal-fix-in-existing-conditional pattern would help both paths.

**Changes applied:**
- `scaffold/.claude/rules/discovered-patterns.md` — This is a general wolfSSL engineering pattern that applies across many subsystems — any PR that tightens validation (certificate checks, domain matching, ASN parsing constraints) faces the same archit
- `scaffold/.claude/rules/investigation-methods.md` — The catastrophic failure was fabricating an entire implementation (variable names, line numbers, test file changes) without reading src/internal.c. The existing evidence rules cover assertions about s

