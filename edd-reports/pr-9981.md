# Eval Report: PR #9981

**Repo:** wolfssl/wolfssl
**PR Title:** Fenrir fixes
**Problem:** Fenrir fixes (source: pr_body)
**Generated:** 2026-03-22T22:34:58Z
**Verdict:** neutral

## Verdicts

| Pillar | Bare | wolfDen | wolfDen (iter 1) |
| --- | --- | --- | --- |
| **Overall** | **fail** | **fail** | **fail** |

## Analysis Details

### Baseline (bare: fail, wolfden: fail)

**Summary:** The task was to identify and implement 'Fenrir fixes' — a batch of security-hardening changes found by static analysis across the wolfSSL codebase. The actual PR (9981) touched 14 files with fixes spanning: error propagation refactors in ed25519/ed448 signing paths (removing dangerous early returns that skip buffer cleanup), ForceZero additions for sensitive cryptographic intermediates (seeds, nonces, private key material), mp_clear→mp_forcezero upgrades for bignum private values, init return value checking in ASN key detection, session deserialization bounds fixes, constant-time padding validation in EVP, cleanup path corrections in ECCSI, a file handle leak in test code, and CI workflow version bumps.

Bare Claude identified 2 of 14 files correctly (dilithium.c ForceZero, pk_rsa.c) but fabricated the specific nature of the pk_rsa.c fix (called it a timing comparison fix rather than mp_clear→mp_forcezero), invented 3 additional dilithium.c locations that don't exist, fabricated an XMEMCMP→ConstantCompare fix in internal.c, and hallucinated a bounds check in wc_ecc_import_x963_ex2.

wolfDen Claude performed worse despite having domain knowledge loaded. It fabricated 9 specific Fenrir issue numbers and 7 file names, achieving zero overlap with the actual PR. Files like wolfcaam_hash.c, kcapi_hash.c, stm32.c, devcrypto_rsa.c, and silabs_ecc.c do not appear in the PR at all. The specific issue numbers (#593, #597, etc.) gave false authority to completely fabricated findings. This is the worst possible outcome: confident, detailed, and entirely wrong.

wolfDen's domain knowledge provided no advantage here. The knowledge files cover platforms, products, and build patterns but apparently lack coverage of: (1) Fenrir as a tool and how to investigate its findings, (2) wolfSSL's error propagation conventions in crypto paths, (3) sensitive buffer lifecycle rules (which values need ForceZero, where mp_forcezero replaces mp_clear), and (4) init/free pairing discipline. These are cross-cutting security patterns that recur across many PRs and would be high-value additions to the knowledge base. The key systemic gap is that without understanding Fenrir's nature as a static analyzer producing specific code-location findings, both Claudes resorted to fabrication rather than investigating the actual codebase.

### Iteration 1 (bare: fail, wolfden: fail)

**Summary:** The task was to investigate and implement 'Fenrir fixes' — a batch of security-hardening changes found by static analysis (Fenrir) across the wolfSSL codebase. The actual PR #9981 touched 14 files with diverse fix types: error propagation refactors removing dangerous early returns in ed25519/ed448 signing paths, ForceZero additions for sensitive cryptographic intermediates (seeds, nonces, private keys), mp_clear→mp_forcezero upgrades for bignum private values, init return value checking in ASN key detection, session deserialization bounds fixes, constant-time padding validation in EVP, cleanup path corrections in ECCSI, a file handle leak fix in test code, and CI workflow version bumps.

Bare Claude produced a response identifying 6 supposed fixes across 3 files. Of these, only one was partially correct (dilithium_make_key ForceZero for seed). It fabricated an XMEMCMP→ConstantCompare timing fix in internal.c (the real changes were return value corrections and memory free reordering), misidentified the pk_rsa.c fix type and location entirely, invented 3 additional dilithium.c ForceZero locations that don't exist in the PR, and hallucinated a bounds check in wc_ecc_import_x963_ex2. The fabrications were presented with high confidence including specific line numbers.

wolfDen Claude performed even worse despite having domain knowledge loaded. It interpreted 'Fenrir' as a runtime system to check logs for rather than recognizing it as a static analysis tool, and simply asked for clarification without investigating the codebase at all. While this avoided the data fabrication that plagued bare Claude, it produced zero useful output — the worst outcome for a knowledge-augmented system that should have been able to reason about what Fenrir-class fixes look like.

The key systemic gap is that wolfDen's knowledge base covers platforms, products, and build patterns but lacks coverage of wolfSSL's security engineering patterns: the 'no early returns in crypto paths' convention that dominates ed25519/ed448 fixes, sensitive buffer lifecycle rules (what needs ForceZero, where mp_forcezero replaces mp_clear), init/free pairing discipline, and crucially, what Fenrir is and how its findings translate to code changes. These cross-cutting security patterns recur across many PRs and are the highest-value additions to the knowledge base. Without understanding Fenrir's nature as a static analyzer, both Claudes had no investigative starting point and resorted to fabrication (bare) or inaction (wolfDen).

**Changes applied:**
- `scaffold/.claude/rules/coding-standards.md` — The dominant fix patterns in this PR — and across wolfSSL security hardening generally — are: converting early returns to if-chains for cleanup safety, upgrading mp_clear to mp_forcezero for sensitive
- `scaffold/.claude/rules/crypto/crypto-api.md` — The existing pattern showed init without checking its return value, which directly contradicts the security discipline wolfSSL enforces. Fenrir flags unchecked init return values (seen in asn.c fixes)

